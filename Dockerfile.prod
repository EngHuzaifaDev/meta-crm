FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

ARG NODE_ENV=production
ENV NODE_ENV=production

COPY . .
RUN npm run build

EXPOSE 3004

ENV PORT=3004

CMD ["npm", "start"]
