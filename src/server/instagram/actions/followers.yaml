name: instagram-followers
description: Open followers dialog and extract follower list
actions:
  - id: clickFollowersButton
    action: click
    selector: "//a[.//span[contains(translate(text(), 'FOLLOWERS', 'followers'), 'followers')]]"
    waitAfter: 2000

  - id: waitForFollowersDialog
    action: wait
    selector: "div[role='dialog']"
    timeout: 10000

  - id: waitForListLoad
    action: waitFor
    timeout: 5000

  - id: finalExtract
    action: extractList
    selector: "//div[@role='dialog']//div[contains(@class, 'x1qnrgzn')]//span[contains(@class, '_aad7')]"
